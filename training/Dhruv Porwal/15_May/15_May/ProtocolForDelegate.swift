// For class - protocol - class delegation method

protocol ProtocolForDelegate: AnyObject {
    
    // This protocol will provide methods to define to developer class
    func addsTowNumbers(result: Int) // Here, in the protocol we have just declared the function
    
}

// Why to use protocol in delegate pattern?
// Solution:
